import { FormatHeightPipe } from './format-height.pipe';

describe('FormatHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatHeightPipe();
    expect(pipe).toBeTruthy();
  });
});

import 'package:flutter/material.dart';

import '../map/controls/map_controls.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const MapControls();
  }
}
